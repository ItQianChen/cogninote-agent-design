## Windows x64

Windows 用户：
下载 `{{WINDOWS_INSTALLER}}` 安装。

{{WINDOWS_SIGNING_NOTE}}

便携版：
下载 `{{WINDOWS_PORTABLE}}`，解压后运行 `CogniNote.exe`。

自动更新：
此版本对应 `{{UPDATER_CHANNEL}}` 更新通道。配置了 Tauri updater 密钥时，发布流程会同步更新对应通道的 `latest.json`。
