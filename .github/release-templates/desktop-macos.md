# 知记空间 {{VERSION}} macOS Apple Silicon {{RELEASE_KIND}}

macOS Apple Silicon 用户：
下载 `{{MACOS_DMG}}` 安装。

{{MACOS_SIGNING_NOTE}}

App 压缩包：
`{{MACOS_APP_ZIP}}` 主要用于排查和归档。

自动更新：
此版本对应 `{{UPDATER_CHANNEL}}` 更新通道。配置了 Tauri updater 密钥时，发布流程会同步更新对应通道的 `latest.json`。
